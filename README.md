# Combat Robot Bite Visualizer

An interactive browser-based tool for visualizing combat-robot spinner bite geometry, tip speed, stored energy, and tooth engagement paths against uploaded STL armor.

## Live site

The GitHub Pages deployment is built automatically from the `main` branch.

## Development

```sh
npm ci
npm run build:pages
```

The static site is written to `pages-dist/`.
